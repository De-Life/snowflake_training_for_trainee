# Terraformプロバイダーバイナリ（容量が大きくGit管理不要）
terraform/.terraform/
terraform/providers/

# stateファイル（Terraform Cloudで管理するため除外）
*.tfstate
*.tfstate.backup

# 認証情報を含む変数ファイル
terraform/terraform.tfvars
terraform/*.tfvars

# 秘密鍵（絶対にGitに含めない）
*.p8
*.pem

#鍵生成用.pyファイル
src/keygen.py